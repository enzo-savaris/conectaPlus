import { ChangeDetectionStrategy, Component } from '@angular/core';

/** Tela provisória: listagem de vagas ainda será implementada. */
@Component({
  selector: 'app-vagas',
  templateUrl: './vagas.html',
  changeDetection: ChangeDetectionStrategy.OnPush
})
export class Vagas {}
