import { ChangeDetectionStrategy, Component } from '@angular/core';

/** Tela provisória: dados de perfil ainda serão implementados. */
@Component({
  selector: 'app-perfil',
  templateUrl: './perfil.html',
  changeDetection: ChangeDetectionStrategy.OnPush
})
export class Perfil {}
