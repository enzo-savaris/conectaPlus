import { ChangeDetectionStrategy, Component } from '@angular/core';

/** Tela provisória: catálogo de cursos ainda será implementado. */
@Component({
  selector: 'app-curso',
  templateUrl: './curso.html',
  changeDetection: ChangeDetectionStrategy.OnPush
})
export class Curso {}
